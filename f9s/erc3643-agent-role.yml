apiVersion: beta/v1
kind: Deployment

metadata:
  name: erc3643-agent-role-deployment
  version: 1.0.0
  description: "ERC3643 AgentRole package deployment"

spec:
  package:
    name: AgentRole

  deployer:
    wallet:
      type: "privateKey" # Options: privateKey, mnemonic, keystore
      value: "${PRIVATE_KEY}" # Environment variable reference

  network:
    chainId: "${CHAIN_ID}" # Blockchain chain ID
    rpcUrl: "${RPC_URL}" # Blockchain RPC
